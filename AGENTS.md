# NMES-Final 项目上下文

## 项目摘要与当前状态

- 项目名称：`NMES-Final`。
- 项目用途、交付目标和技术栈尚未确认，不根据仓库名称推断需求。
- 当前阶段：已建立项目记忆与文档骨架，尚无业务代码、依赖配置或运行入口。
- 开发位置：WSL `Ubuntu-E`，路径 `/home/pc/project/NMES-Final`。
- 远程仓库：`https://github.com/Li-Lazenca-Qiuqi/NMES-Final.git`。

## 开始工作时

1. 阅读用户级规则、本文件，以及工作目录下适用的子目录 `AGENTS.md`。
2. 检查 `git status --short --branch` 和近期 Git 记录；首次提交前无历史属于正常情况。
3. 阅读 [当前待办](doc/TODO.md) 与 [路线图](doc/Roadmap.md)，按任务需要查看需求、决策和专题笔记。
4. 核实相关文档是否与代码一致，区分已验证事实、计划和待确认事项。
5. 默认不读取 `doc/archive/`；只有追溯历史依据时按需访问。

## 目录地图与索引

| 路径 | 用途 |
| --- | --- |
| `README.md` | 对外介绍、稳定能力与实际可用的使用方式 |
| `AGENTS.md` | 项目摘要、当前状态、索引与长期约定 |
| `doc/AGENTS.md` | 文档维护规则 |
| `doc/TODO.md` | 唯一当前待办入口与完成状态 |
| `doc/Roadmap.md` | 阶段目标、依赖与验收条件 |
| `doc/PRD/` | 需求、范围与验收标准 |
| `doc/decisions/` | 重要选型、依据及取舍 |
| `doc/note/` | 有效的专题知识与验证依据 |
| `doc/archive/` | 已失效或已被替代的历史材料 |

## 长期约定

- 本项目位于 WSL，项目命令通过 `wsl.exe -d Ubuntu-E` 进入后使用 Bash，使用 WSL 内工具链。Python 环境采用 uv；其他技术栈按确认后的需求选择。
- 文档与必要代码注释使用简体中文，代码标识符与路径使用英语。实现应模块化，函数说明功能、输入、输出及必要约束。
- 不将令牌、密码、私钥或含凭据的配置写入仓库；新增依赖和生成产物时维护对应忽略规则。
- 修改前保留并识别用户已有改动。删除操作须再次确认并获得许可。
- 仅在用户明确要求时提交 Git；提交按改动类型拆分，标题使用 `feat`、`fix`、`doc` 等类别，正文使用简体中文，末尾保留 `Generated with Codex`。
- 仅在用户明确要求更新项目版本时创建或更新 `CHANGELOG.md`，采用 `major.minor.patch`，不把日常工作写成版本发布。
- 不使用 `LAST_RUN.md`。待办写入 `doc/TODO.md`，阶段目标写入路线图，长期结论写入对应专题或决策文档。

## 完成工作时

1. 执行与改动范围相符的检查，记录真实结果和未解决的问题；无可执行代码时检查文档链接、结构与一致性。
2. 更新待办状态及相关需求、决策或专题文档；项目状态或目录发生变化时同步本文件。
3. 用户可见能力或使用方式稳定变化时同步 `README.md`，不要填入未经验证的安装或运行命令。
4. 报告交付内容、验证结果和阻塞项。临时文件需清理时遵循删除确认规则。
